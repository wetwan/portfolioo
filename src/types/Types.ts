export type Navmenu = {
  link: string;
  label: string;
};
export type Aboutindex = {
  label: string;
  index: number;
};
export type Skillindex = {
  name: string;
  percent: number;
};
export type Serviceindex = {
  image: string;
  title: string;
  description: string;
};
export type Portfolioindex = {
 
    image: string,
    title: string,
    stack:string[] ,
    link: string
 
};
